Я работаю на фреймворке Laravel 11. Мне нужно реализовать правильное кадрирование (crop) и изменение размера (resize) загружаемых изображений.

Напиши пошаговое руководство и чистый код, соответствующий современным стандартам Laravel, используя библиотеку Intervention Image (версия 3).

### Моя задача:
У меня есть оригинальное изображение. Мне нужно обработать его так, чтобы на выходе получилась картинка с точной шириной 1540px. При этом кадрирование должно быть "умным" (центрированным или по содержимому), чтобы пропорции не искажались (без растягивания и сжатия), а лишние края обрезались.

### Что должно быть в ответе:
1. Команда для установки актуальной версии библиотеки Intervention Image через Composer и базовая настройка драйвера (GD или Imagick) в Laravel.
2. Сервисный класс или метод в контроллере для обработки загружаемого файла (`Illuminate\Http\UploadedFile`).
3. Код должен использовать метод `cover()` или `crop()` из Intervention Image v3 для правильного кадрирования под 1540px с сохранением Aspect Ratio.
4. Сохранение готового результата в кастомную папку внутри `Storage::disk('public')` с генерацией уникального имени (например, через UUID или timestamp) и обязательным приведением к формату .webp или .jpg для оптимизации.
5. Обработка базовых ошибок (try-catch), если файл поврежден.

Пиши код с использованием строгой типизации (PHP 8.2+), без лишней "воды", только качественный production-ready код и краткие комментарии к нему.